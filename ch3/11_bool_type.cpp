#include <iostream>
using namespace std;

// we have special integer type which is boolean
// it sotres only 1 or 0
// --> 1 represents true, 0 represents false
// traditionally, 0 is false, all others true
int main() {
    bool is_ready = true; // true : 1 flase : 0
    int ans = true;
    int promise = false;

    // 0 represents false, all other numbers represent true
    bool start = -100;  // stop assigned true
    bool stop = 0;      // stop assigned false
    return 0;
}
